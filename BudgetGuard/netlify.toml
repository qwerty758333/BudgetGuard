# Canonical Netlify config is at repo root: ../netlify.toml (sets base = "BudgetGuard").
# Vite inlines env vars at build time — set in Netlify UI:
#   VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_ADMIN_EMAIL, VITE_ADMIN_USER_IDS
